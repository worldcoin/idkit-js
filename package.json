{
	"private": true,
	"version": "1.0.0-alpha.4",
	"name": "idkit-monorepo",
	"scripts": {
		"dev": "turbo run dev",
		"lint": "turbo run lint",
		"typecheck": "find ./packages/ -type d -exec sh -c 'pnpm typecheck' \\;",
		"build": "turbo run build",
		"prepublishOnly": "npm run build"
	},
	"devDependencies": {
		"turbo": "^1.10.16",
		"typescript": "^5.3.2"
	},
	"engines": {
		"node": ">=18"
	}
}
