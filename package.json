{
	"private": true,
	"version": "1.2.2",
	"name": "idkit-monorepo",
	"scripts": {
		"dev": "turbo run dev",
		"lint": "turbo run lint",
		"build": "turbo run build",
		"prepublishOnly": "npm run build"
	},
	"devDependencies": {
		"turbo": "^1.13.0"
	},
	"engines": {
		"node": ">=18"
	}
}
